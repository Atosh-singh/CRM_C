const { createRole } = require("./createRole");
const { getRoles } = require("./getRoles");
const { updateRole } = require("./updateRole");
const { deleteRole } = require("./deleteRole");
const { restoreRole } = require("./restoreRole");

module.exports = {
  createRole,
  getRoles,
  updateRole,
  deleteRole,
  restoreRole
};
 

