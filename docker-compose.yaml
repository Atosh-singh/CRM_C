version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: car-app
    restart: unless-stopped
    ports:
      - "5000:5000"
    env_file:
      - car-company-system/.env